require"nvim-tree".setup {
    auto_close         = true,
    open_on_setup      = true,
    git = {
        enable = true,
    },
    view = {
        side             = "left",
        number           = true,
        relativenumber   = true,
        auto_resize      = true,
    }
}
